apiVersion: v1
kind: Service
metadata:
  name: podsense-service
  namespace: default
spec:
  selector:
    app: podsense-pods
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8501
      nodePort: 30001
